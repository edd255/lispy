/// @file prompt.h
/// @brief This file contains the method to print the prompt.

#pragma once

#ifndef LISPY_UTILS_PROMPT_H
#define LISPY_UTILS_PROMPT_H

/// @brief Prints the prompt for the command-line interpreter.
void print_prompt(void);

/// @brief Prints the banner for the command-line interpreter.
void print_banner(void);

#endif

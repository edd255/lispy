;==== nth Test ================================================================
(fun {test cond} {
    if (cond)
    { print "PASSED" }
    { print "FAILED" }
})

(test (== (tail "Hello, World!") "ello, World!"))
(test (== (tail "ok") "k"))
(test (== (head "Hello, World!") "H"))
(test (== (head "ok") "o"))
